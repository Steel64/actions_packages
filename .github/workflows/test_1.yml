name: ejemplo de composite action

on : [push]

jobs:
  say-hello:
    runs-on : ubuntu-latest

    env:
      MY_var: Mi variable de enterno a nivel de job

    steps:
      - name: test
        run: echo "valor: $MY_var"